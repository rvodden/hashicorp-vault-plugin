<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:section title="${%vaultConfigurations}">
    <f:entry title="${%vaultName}" field="vaultName">
      <f:textbox />
    </f:entry>
    <f:entry title="${%vaultUrl}" field="vaultUrl">
      <f:textbox />
    </f:entry>
    <f:property field="vaultAuthenticationConfiguration" />
  </f:section>
</j:jelly>
